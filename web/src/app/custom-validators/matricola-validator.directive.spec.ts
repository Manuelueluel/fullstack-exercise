import { MatricolaValidatorDirective } from './matricola-validator.directive';

describe('MatricolaValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatricolaValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
