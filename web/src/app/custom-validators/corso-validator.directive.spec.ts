import { CorsoValidatorDirective } from './corso-validator.directive';

describe('CorsoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CorsoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
